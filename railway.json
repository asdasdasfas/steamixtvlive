{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd app && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd app && npm run preview -- --host 0.0.0.0 --port 8000",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
